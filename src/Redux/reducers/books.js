// Create reducers for the state of the books
const defaultState = {};
const Books = (state = defaultState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default Books;
